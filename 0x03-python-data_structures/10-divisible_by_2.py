#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    checK_div = []

    for i in range(len(my_list)):
        if my_list[i] % 2 == 0:
            check_div.appent(True)
        else:
            chech_div.append(False)

    return (chech_div)
